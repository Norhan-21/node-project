<h1>Departments</h1>

<div class="my-2">
  <a href="/departments/create">Add a new department</a>
</div>

{{#each departments}}

  <div class="d-flex justify-content-between">
        {{name}}
       {{!-- <a href="/departments/{{_id}}"> <button type="submit">Details</button></a> --}}
      



     {{!-- <a href="/departments/{{_id}}">
      {{name}}
     </a> --}}

    <div>

        <a href="/departments/{{_id}}"> <button type="submit">Details</button></a>
      <a href="/departments/{{_id}}/edit"> <button type="submit">Edit</button></a>

      {{!-- <a href="/departments/{{_id}}/edit">
        Edit
      </a> --}}

      <form action="/departments/{{_id}}?_method=DELETE" method="post">
        <button type="submit"onclick="return confirm('Are you sure to delete?')">
            
            
            Delete</button>
      </form>

    </div>

  </div>
{{/each}}
<div>
  <button onclick="window.location.href = 'http://localhost:3000/adminchoices'">Go Back</button>
</div>
